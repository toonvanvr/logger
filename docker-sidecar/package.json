{
  "name": "@logger/docker-sidecar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/main.ts",
    "dev": "bun run --watch src/main.ts",
    "build": "bun build src/main.ts --target=bun --outdir=dist"
  },
  "dependencies": {
    "@logger/client": "file:../client"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
