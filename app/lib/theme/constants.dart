import 'package:flutter/material.dart';

/// Standard border radii used throughout the app.
const kBorderRadius = BorderRadius.all(Radius.circular(4));
const kBorderRadiusSm = BorderRadius.all(Radius.circular(3));
const kBorderRadiusLg = BorderRadius.all(Radius.circular(6));

/// Overlay scrim used behind panels (25% black).
const kScrimColor = Color(0x40000000);

/// Subtle error hint background tint.
const kErrorHintBg = Color(0x15E06C60);

/// Semantic font sizes used throughout the app.
const kFontSizeBadge = 8.0;
const kFontSizeLabel = 9.0;
const kFontSizeBody = 10.0;
const kFontSizeSubhead = 11.0;
const kFontSizeControl = 12.0;
const kFontSizeIcon = 14.0;

/// Common padding values.
const kHPadding8 = EdgeInsets.symmetric(horizontal: 8);
const kHPadding12 = EdgeInsets.symmetric(horizontal: 12);
const kVPadding4 = EdgeInsets.symmetric(vertical: 4);
